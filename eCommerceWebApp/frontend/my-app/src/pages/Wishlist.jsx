import { NavLink } from "react-router-dom";

const Wishlist = () => {
  return (
    <section>
      <NavLink to="/user-menu">Back</NavLink>
      <h1>Wishlist Page</h1>
    </section>
  );
};

export default Wishlist;
