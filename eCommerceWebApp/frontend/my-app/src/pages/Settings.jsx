import { NavLink } from "react-router-dom";

const Settings = () => {
  return (
    <section>
      <NavLink to="/user-menu">Back</NavLink>
      <h1>Settings Page</h1>
    </section>
  );
};

export default Settings;
